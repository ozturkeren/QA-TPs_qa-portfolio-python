[pytest]
addopts = -q
testpaths = tests
markers =
    ui: UI/browser tests
    api: API tests
    sql: SQL tests
    demo_fail: intentionally failing UI tests to verify Allure screenshots; skipped in CI